<!DOCTYPE html>

<html>
<head>
  <title>Real time chat with RoR and Cassandra </title>
  <%= favicon_link_tag 'favicon.ico' %>
  <%= stylesheet_link_tag 'chat' %>
</head>

<body>
<section class="login-box">
  <header class="login-header">
    <h1>Chat user selection</h1>
  </header>
  <ol class="selection-box">
    <% @users.each do |user| %>
      <% user_url_encoded = user.user_id.to_s %>
      <%= link_to "user/login/#{user_url_encoded}" do %> 
        <li>
          <div class="user-image">
            <%= render_profile_pic(user) %>
          </div> 
          <p class="user-name"><%= user.username %></p>
        </li>  
      <% end %>
    <% end %>
  </ol>
  <div class="copyright"> Juan Lizarazo & Devan Allen &copy; <%= Time.now.year %></div>
</section>
</body>
</html>
